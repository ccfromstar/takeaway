<!--页面逻辑-->
<% include header %>
<table class="tbpage">
    <tr>
      <td>
      		<button type="button" class="btn btn-default btn-xs" id="table_new" onclick="newDoc()" >新增</button>
			<table class="table table-hover" id="table_list">
    		<thead>
      			<tr>
        			<th>序号</th>
        			<th>日期</th>
        			<th>公司</th>
        			<th>金额</th>
        			<th>操作</th>
      			</tr>
    		</thead>
    		<tbody>
    		<%for(var i in address){%>
      			<tr>
        			<td><%-address[i].id%></td>
        			<td><%=address[i].date%></td>
        			<td><%=address[i].name%></td>
        			<td><%=address[i].priceTotal%></td>
        			<td>
        				<button type="button" class="btn btn-default btn-xs" onclick="editDoc(<%=address[i].id%>)">编辑</button>
        				<button type="button" class="btn btn-default btn-xs" onclick="delinfo(<%=address[i].id%>)">删除</button>
        			</td>
      			</tr>
    		<%}%>
    		</tbody>
    		</table>
    		<!--分页符-->
			<div style='width:100%;text-align:center'>
  				<button type='button' <%=isFirstPage?"disabled":""%> <%=isFirstPage?"":"onclick=toPage("+(page-1)+");"%> class='btn btn-default btn-xs'>上一页</button>
  				当前第<%=page%>页/共<%=totalpage%>页&nbsp;<%=total%>条数据
  				<button type='button' <%=isLastPage?"disabled":""%> <%=isLastPage?"":"onclick=toPage("+(page+1)+");"%> class='btn btn-default btn-xs'>下一页</button>
			</div>  
			<br/><br/>  	
      </td>
    </tr>
</table>
<!--table footer -->
	  </td>
    </tr>
  </table>

<!-- 模态框（Modal） -->
<div class="modal fade" id="addModal" tabindex="-1" role="dialog" 
   aria-labelledby="myModalLabel" aria-hidden="true">
   <div class="modal-dialog" style="width:800px" >
      <div class="modal-content">
         <div class="modal-body">
            <table class="table table-bordered">
                <tr>
                    <td>日期</td>
                    <td>
                      <input type="text" name="date" id="date" style="cursor:pointer" readonly  class="Wdate" onfocus="WdatePicker({doubleCalendar:false,dateFmt:'yyyy-MM-dd'})"></input>
                    </td>
                    <td>数量</td>
                    <td><input type="text" class="input-sm form-control" id="num" onkeyup="value=value.replace(/[^\d]/g,'')"></input></td>
                </tr>   
                <tr>
                    <td>单价</td>
                    <td><input type="text" class="input-sm form-control" id="unitPrice" onkeyup="value=value.replace(/[^\d]/g,'')"></input></td>
                    <td>负责人</td>
                    <td><input type="text" class="input-sm form-control" id="manager" ></input></td>
                </tr>
                <tr>
                    <td>企业名称</td>
                    <td><input type="text" class="input-sm form-control" id="name" ></input></td>
                    <td>总金额</td>
                    <td><input type="text" class="input-sm form-control" id="priceTotal" onkeyup="value=value.replace(/[^\d]/g,'')"></input></td>
                </tr>
                <tr>
                    <td>公司抬头</td>
                    <td><input type="text" class="input-sm form-control" id="head" ></input></td>
                    <td>支付状态</td>
                    <td><input type="text" class="input-sm form-control" id="state" ></input></td>
                </tr>
                <tr>
                    <td>支付方式</td>
                    <td>
                       <select id="type" class="input-sm selectpicker">
                            <option value="-">请选择</option>
                            <option value="微信">微信</option>
                            <option value="支付宝">支付宝</option>
                            <option value="现金">现金</option>
                            <option value="公司转账">公司转账</option>
                            <option value="POS机">POS机</option>
                        </select>
                    </td>
                    <td></td>
                    <td></td>
                </tr>
             </table>
             <input type="hidden" id="docid" ></input>
         </div>
         <div class="modal-footer">
            <button type="button" class="btn btn-default" 
               data-dismiss="modal">关闭
            </button>
            <button type="button" class="btn btn-primary" onclick="submitDoc();">
               提交
            </button>
         </div>
      </div><!-- /.modal-content -->
</div><!-- /.modal -->

<script type="text/javascript">
	function newDoc(){
		$("#date").val("");
		$("#num").val("");
		$("#unitPrice").val("");
		$("#manager").val("");
		$("#name").val("");
		$("#priceTotal").val("");
		$("#head").val("");
		$("#state").val("");
		$("#type").val("");
		$("#docid").val("0");

		$('#addModal').modal('show');
	}

  function toPage(i){
    window.location = "/cms/old?p="+i;
  }

	function editDoc(id){
        //根据id读取字段
        $.ajax({ type:"POST" , url: "/old/getinfo", data:{id:id}, success: function(data){
               $("#date").val(data[0].date); 
               $("#num").val(data[0].num); 
               $("#unitPrice").val(data[0].unitPrice);
               $("#manager").val(data[0].manager);
               $("#name").val(data[0].name);
               $("#priceTotal").val(data[0].priceTotal);
               $("#head").val(data[0].head);
               $("#state").val(data[0].state);
               $("#type").val(data[0].type);
               $("#docid").val(id);
        }});
        $('#addModal').modal('show');
    }

	function submitDoc(){
		if($("#docid").val()=="0"){
			//insert
			$.ajax({ type:"POST" , url: "/old/insert", data:{
                date:$("#date").val(),
                num:$("#num").val(),
                unitPrice:$("#unitPrice").val(),
                manager:$("#manager").val(),
                name:$("#name").val(),
                priceTotal:$("#priceTotal").val(),
                head:$("#head").val(),
                state:$("#state").val(),
                type:$("#type").val()
                }, success: function(data){
                    if(data==200){
                        $('#addModal').modal('hide');
                        window.location.reload();
                    }
        	}});
		}else{
			//update
			$.ajax({ type:"POST" , url: "/old/update", data:{
                date:$("#date").val(),
                num:$("#num").val(),
                unitPrice:$("#unitPrice").val(),
                manager:$("#manager").val(),
                name:$("#name").val(),
                priceTotal:$("#priceTotal").val(),
                head:$("#head").val(),
                state:$("#state").val(),
                type:$("#type").val(),
                docid:$("#docid").val()
                }, success: function(data){
                    if(data==200){
                        $('#addModal').modal('hide');
                        window.location.reload();
                    }
        	}});
		}
    }

    function delinfo(id){
    	$.confirm({
    		title: '请确认',
    		content: '是否确认删除？',
    		confirm: function(){
        		$.ajax({ type:"POST" , url: "/old/delinfo", data:{id:id}, success: function(data){
        			if(data==200){
              			window.location.reload();     
        			}
      			}});	
    		}
		});
    }

<%if(info){%>
    $.alert({title: '提示信息',content: '<%=info%>'});
<%}%>
</script>
<% include footer %>